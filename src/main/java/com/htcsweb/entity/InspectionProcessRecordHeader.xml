<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.htcsweb.dao.InspectionProcessRecordHeaderDao">


    <insert id="addInspectionProcessRecordHeader" parameterType="com.htcsweb.entity.InspectionProcessRecordHeader">
        INSERT INTO inspection_process_record_header(inspection_process_record_header_code,process_code,pipe_no,operation_time,operator_no,upload_files,remark,result,mill_no) VALUES (#{inspection_process_record_header_code},#{process_code},#{pipe_no},#{operation_time},#{operator_no},#{upload_files},#{remark},#{result},#{mill_no})
    </insert>

    <update id="updateInspectionProcessRecordHeader" parameterType="com.htcsweb.entity.InspectionProcessRecordHeader">
        UPDATE inspection_process_record_header set inspection_process_record_header_code=#{inspection_process_record_header_code},
        process_code=#{process_code},
        pipe_no=#{pipe_no},operation_time=#{operation_time},operator_no=#{operator_no},
        upload_files=#{upload_files},remark=#{remark},result=#{result},mill_no=#{mill_no} where id=#{id}
    </update>

    <delete id="delInspectionProcessRecordHeader">
        delete from inspection_process_record_header WHERE
        <foreach collection="array" item="item" open="id in (" separator="," close=")">
            #{item}
        </foreach>
    </delete>


    <select id="getCountAllByLike" resultType="int">
        select count(*) from inspection_process_record_header o
        inner join process_info pc on pc.process_code=o.process_code
        left join pipe_basic_info p on p.pipe_no=o.pipe_no
        left join contract_info c on p.contract_no=c.contract_no
        left join project_info pro on c.project_no=pro.project_no
        left join pipe_status s on s.status_code=p.status
        <where>
            <if test="pipe_no!=null and pipe_no!=''">
                AND o.pipe_no LIKE CONCAT('%',#{pipe_no},'%')
            </if>
            <if test="operator_no!=null and operator_no!=''">
                AND o.operator_no LIKE CONCAT('%',#{operator_no},'%')
            </if>
            <if test="mill_no!=null and mill_no!=''">
                AND o.mill_no =#{mill_no}
            </if>
            <if test="process_code!=null">
                AND o.process_code=#{process_code}
            </if>
            <if test="begin_time!=null">
                <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%T:%s')>=DATE_FORMAT(#{begin_time},'%Y-%m-%d 00:00:00')]]>
            </if>
            <if test="end_time!=null">
                <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%T:%s')<DATE_FORMAT(DATE_ADD(#{end_time},INTERVAL 1 DAY),'%Y-%m-%d 00:00:00')]]>
            </if>
        </where>
    </select>


    <select id="getAllByLike" resultType="map">
        Select pc.process_name,pc.process_name_en,pro.project_name,s.status_name,p.contract_no,p.grade,p.od,p.wt,p.p_length,p.weight,p.heat_no,o.*
        from inspection_process_record_header o
        inner join process_info pc on pc.process_code=o.process_code
        left join pipe_basic_info p on p.pipe_no=o.pipe_no
        left join contract_info c on p.contract_no=c.contract_no
        left join project_info pro on c.project_no=pro.project_no
        left join pipe_status s on s.status_code=p.status
        <where>
            <if test="pipe_no!=null and pipe_no!=''">
                AND o.pipe_no LIKE CONCAT('%',#{pipe_no},'%')
            </if>
            <if test="operator_no!=null and operator_no!=''">
                AND o.operator_no LIKE CONCAT('%',#{operator_no},'%')
            </if>
            <if test="process_code!=null">
                AND o.process_code=#{process_code}
            </if>
            <if test="mill_no!=null and mill_no!=''">
                AND o.mill_no =#{mill_no}
            </if>
            <if test="begin_time!=null">
                <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%T:%s')>=DATE_FORMAT(#{begin_time},'%Y-%m-%d 00:00:00')]]>
            </if>
            <if test="end_time!=null">
                <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%T:%s')<DATE_FORMAT(DATE_ADD(#{end_time},INTERVAL 1 DAY),'%Y-%m-%d 00:00:00')]]>
            </if>
        </where>
        ORDER BY o.id DESC
        limit #{skip},#{take}
    </select>





    <select id="getInspectionProcessRecordHeaderById" resultType="com.htcsweb.entity.InspectionProcessRecordHeader">
        select * from inspection_process_record_header where id=#{id}
    </select>

    <!--获取当天某工位记录-->
    <select id="getProcessRecord" resultType="map">
        Select  o.*
        from inspection_process_record_header o
        left join pipe_basic_info p on p.pipe_no=o.pipe_no
        left join contract_info c on p.contract_no=c.contract_no
        left join project_info pro on c.project_no=pro.project_no
        WHERE pro.project_no=#{project_no} AND o.mill_no=#{mill_no}
        AND format(c.od,3)=format(#{od},3)
        AND format(c.wt,3)=format(#{wt},3)
        <if test="process_code!=null">
            AND process_code=#{process_code}
        </if>
        <if test="begin_time!=null">
            <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%i:%s')>=DATE_FORMAT(#{begin_time},'%Y-%m-%d %H:%i:%s')]]>
        </if>
        <if test="end_time!=null">
            <![CDATA[ AND DATE_FORMAT(o.operation_time,'%Y-%m-%d %H:%i:%s')<DATE_FORMAT(#{end_time},'%Y-%m-%d %H:%i:%s')]]>
        </if>
        ORDER BY o.id DESC
    </select>

    <!--根据钢管编号查询最新一条记录-->
    <select id="getRecentRecordByPipeNo" resultType="com.htcsweb.entity.InspectionProcessRecordHeader">
        SELECT  * from inspection_process_record_header where pipe_no=#{pipe_no} and process_code=#{process_code} order by operation_time desc LIMIT 1
    </select>


    <!--根据钢管编号查询之前的最新一条合格记录-->
    <select id="getLastAcceptedRecordBeforePipeNo" resultType="com.htcsweb.entity.InspectionProcessRecordHeader">
        select * from inspection_process_record_header
        <where>
            <if test="pipe_no!=null and pipe_no!=''">
                AND  <![CDATA[  id < (select id from inspection_process_record_header where pipe_no = #{pipe_no} order by id desc limit 1) ]]>
            </if>
            AND result ='1' and mill_no=#{mill_no} and process_code=#{process_code}
        </where>
        order by id desc
        limit 1
    </select>


</mapper>